# Data Sources

Systems and platforms commonly accessed for data analysis and business intelligence.

## Business Intelligence Platforms

- **Tableau** -- dashboards (dashboard ID, title, sheets, layout, device layouts, filters, actions, parameters, tooltips, publish status), workbooks (workbook ID, name, project, owner, views, data sources, tags, permissions, last updated, site), data extracts (extract ID, data source, schedule, refresh mode, incremental/full, rows, size, last refreshed, connection type), calculated fields (field name, formula, data type, table, LOD expression, dependencies, usage in sheets), Tableau Prep (flow ID, steps, inputs, joins, unions, pivots, aggregations, outputs, run history, published data source), Tableau Server/Cloud (site, users, groups, projects, permissions, schedules, subscriptions, usage statistics, background tasks)
- **Power BI** -- reports (report ID, name, workspace, pages, visuals, filters, slicers, bookmarks, drill-through, publish status, endorsement), dashboards (dashboard ID, name, tiles, data sources, pinned visuals, alerts, Q&A, sharing, mobile layout), datasets (dataset ID, name, tables, relationships, measures, calculated columns, refresh schedule, row-level security, endorsement, sensitivity label), dataflows (dataflow ID, name, entities, queries, refresh schedule, linked entities, computed entities, storage type), DAX queries (measure name, formula, dependencies, data type, table, format string, calculation group, time intelligence)
- **Looker (Google Cloud)** -- LookML models (model name, explores, views, dimensions, measures, relationships, access grants, data tests, refinements), Explores (explore name, view, joins, dimensions, measures, filters, always_filter, sql_always_where, access_filter), dashboards (dashboard ID, title, tiles, filters, cross-filters, layout, LookML dashboard/user dashboard, scheduling), scheduled reports (schedule ID, dashboard/look, recipients, frequency, format, filters, conditions, timezone, datagroup), embedded analytics (embed URL, SSO, permissions, user attributes, API, SDK, actions, scheduled plans)
- **Metabase** -- questions (question ID, name, type, database, table, query, visualization, filters, parameters, saved, collection), dashboards (dashboard ID, name, cards, filters, parameters, click behavior, text cards, auto-refresh, caching), collections (collection ID, name, parent, items, permissions, pinned, personal), SQL queries (query text, database, variables, template tags, result metadata, caching, snippet), data models (table, columns, visibility, relationships, metrics, segments, custom columns, formatting)
- **Sigma Computing** -- spreadsheet-like BI (workbook ID, pages, elements, data sources, version, sharing, scheduling, controls), live queries (data source, warehouse, tables, columns, calculations, filters, grouping, live connection, caching), workbooks (workbook ID, name, pages, datasets, version history, published, draft, permissions, embedding), collaborative analytics (comments, shared workbooks, input tables, write-back, version history, change tracking, approval workflow)

## Data Warehouses & Lakes

- **Snowflake** -- data warehouse (database, schema, tables, views, stages, file formats, sequences, procedures, functions, tasks, streams), schemas (schema name, tables, views, functions, procedures, access control, managed access, data retention), stages (stage name, type, URL, storage integration, file format, directory table, encryption), Snowpipe (pipe name, auto-ingest, definition, copy statement, error handling, notification channel, history), time travel (object, retention period, AT/BEFORE, undrop, clone, historical query, fail-safe), data sharing (share name, databases, schemas, tables, views, consumers, reader accounts, secure views), Snowpark (session, DataFrame, UDFs, stored procedures, ML models, Java/Python/Scala, deployment)
- **Google BigQuery** -- serverless analytics (project, dataset, tables, views, routines, materialized views, external tables, access control), SQL queries (query text, execution plan, bytes scanned, slots used, cache hit, query plan, stage statistics, bi-engine acceleration), ML models (model name, model type, training data, evaluation metrics, prediction query, feature importance, hyperparameters), streaming (table, streaming buffer, insertAll, committed rows, estimated rows, write API), federated queries (external data source, connection, external table, query pushdown, data transfer)
- **Amazon Redshift** -- columnar storage (cluster, database, schema, tables, columns, distribution key, sort key, encoding, compression), Spectrum external tables (external schema, external table, S3 location, file format, partitions, column mapping), materialized views (MV name, definition, refresh, auto-refresh, stale, incremental, base tables), AQUA (acceleration status, eligible queries, cache utilization, performance gain, workload analysis)
- **Databricks** -- Lakehouse (workspace, catalog, schemas, tables, views, volumes, functions, models, notebooks, jobs, clusters), Delta Lake (table, schema, partitions, Z-order columns, optimize, vacuum, time travel, change data feed, liquid clustering), SQL analytics (SQL warehouse, queries, dashboards, alerts, query history, warehouse size, auto-scaling, serverless), notebooks (notebook ID, language, cells, results, version, comments, scheduling, widgets, parameters), Unity Catalog (catalog, schemas, tables, views, functions, models, volumes, permissions, lineage, data classification, tags)
- **Azure Synapse Analytics** -- dedicated SQL pools (pool name, DWU, tables, distributions, indexes, statistics, workload management, result set caching), serverless SQL pools (external tables, external data source, file format, OPENROWSET, views, CETAS), Spark pools (pool name, node size, nodes, libraries, packages, notebooks, jobs, autoscale), data integration (pipeline, activities, linked services, datasets, triggers, data flows, mapping, monitoring)

## Databases

- **PostgreSQL** -- relational queries (tables, columns, data types, constraints, indexes, views, functions, triggers, schemas, roles), JSON support (jsonb columns, JSON operators, JSON functions, indexing, path queries, containment, existence), extensions (extension name, version, schema, functions, types, operators, installed/available), partitioning (table, partition strategy, partition key, partitions, range/list/hash, pruning, constraint exclusion)
- **MySQL** -- relational data (databases, tables, columns, data types, constraints, indexes, views, stored procedures, functions, triggers), replication (master/replica, binlog, replication lag, GTID, multi-source, parallel replication, semi-synchronous), stored procedures (procedure name, parameters, body, definer, SQL security, character set, collation), performance (slow query log, query execution plan, EXPLAIN, optimizer trace, index usage, buffer pool, InnoDB status)
- **MongoDB** -- document database (databases, collections, documents, fields, indexes, schema validation, capped collections, views), aggregation pipelines (stages, $match, $group, $project, $lookup, $unwind, $sort, $limit, $facet, $merge, $out), Atlas analytics (Analytics Node, connectors, charts, dashboards, data lake, federated queries, online archive), search (Atlas Search, index definition, analyzers, mappings, queries, facets, scoring, synonyms, autocomplete)
- **Microsoft SQL Server** -- T-SQL queries (databases, tables, views, stored procedures, functions, triggers, schemas, synonyms, CLR objects), SSRS reporting (report definition, data sources, datasets, parameters, expressions, charts, tables, matrices, subscriptions), SSIS integration (packages, data flow, control flow, connection managers, transformations, error handling, logging, deployment), performance (execution plans, query store, statistics, indexes, DMVs, wait stats, resource governor, in-memory OLTP)
- **Amazon RDS / Aurora** -- managed relational databases (DB instance, engine, version, instance class, storage type, multi-AZ, backup retention, parameter group), read replicas (replica instance, replication lag, source instance, region, promotion, cross-region, Aurora global database), performance insights (DB load, top SQL, wait events, counter metrics, dimension analysis, time range, period, database engine analysis)

## Data Integration & ETL

- **dbt (data build tool)** -- SQL-based transformations (model name, SQL, materialization, schema, database, tags, meta, config, pre/post hooks), data modeling (model relationships, ref(), source(), ephemeral, incremental, snapshot, seed), testing (test type, model, column, severity, warn_if, error_if, custom tests, generic tests, store_failures), documentation (description, columns, tests, tags, meta, markdown, doc blocks, generated docs site), lineage (model, upstream models, downstream models, source tables, exposure, DAG, cross-project references)
- **Fivetran** -- automated data connectors (connector ID, source, destination, schema, tables, sync frequency, status, setup, authentication), schema management (destination schema, tables, columns, data types, hashing, history mode, sync mode), transformation scheduling (transformation, schedule, dbt project, dbt models, run history, status, logs)
- **Airbyte** -- open-source data integration (connection ID, source, destination, namespace, stream, sync mode, cursor field, primary key, schedule), connectors (connector type, name, version, specification, configured properties, catalog, status), normalization (raw tables, normalized tables, deduplication, SCD type 2, custom transformations), CDC (change data capture, source, replication slot, publication, LSN, WAL, initial snapshot, incremental sync)
- **Apache Airflow** -- workflow orchestration (DAG ID, schedule, start date, end date, catchup, max active runs, concurrency, tags, owner), DAGs (DAG structure, tasks, dependencies, trigger rules, SLA, callbacks, task groups, SubDAGs), scheduling (scheduler, executor, task instances, queue, pool, priority, retries, retry delay, execution date), task dependencies (upstream, downstream, trigger rule, branching, short-circuit, sensors, external task)
- **Stitch (Talend)** -- data pipeline (integration ID, source, destination, tables, replication method, frequency, status, API usage), cloud connectors (connector name, version, properties, selected tables, columns, replication keys, bookmark), replication (full table, incremental, log-based, replication key, bookmark value, rows synced, last sync, extraction log)

## Spreadsheets & Ad Hoc

- **Google Sheets** -- collaborative spreadsheets (spreadsheet ID, title, sheets, named ranges, owner, editors, viewers, last modified, size), connected sheets (BigQuery data source, tables, pivot tables, charts, calculated columns, extract, scheduled refresh), Apps Script (script ID, functions, triggers, services, execution log, quota usage, deployed web app)
- **Microsoft Excel** -- pivot tables (source data, rows, columns, values, filters, calculated fields, grouping, slicers, timeline, style), Power Query (query name, source, steps, applied transformations, data type, load destination, refresh, error handling), Power Pivot (data model, tables, relationships, DAX measures, calculated columns, KPIs, perspectives, hierarchies), data models (tables, relationships, cardinality, cross-filter direction, inactive relationships, measures, calculated tables), macros (module, procedure, VBA code, event triggers, user forms, error handling, references)
- **Airtable** -- relational spreadsheets (base ID, tables, fields, field types, records, views, linked records, lookup, rollup, formula), views (view type, filters, sorts, groups, field visibility, color, row height, kanban, calendar, gallery, timeline, Gantt), automations (automation ID, trigger, actions, conditions, run history, status, linked records, send email, webhook), interfaces (interface ID, pages, elements, data sources, permissions, published status, layout, actions)

## Statistical & ML Tools

- **Python (pandas, scikit-learn, matplotlib, seaborn)** -- data manipulation (DataFrame, Series, index, columns, dtypes, shape, merge, groupby, pivot, melt, apply, rolling, resample), statistical analysis (descriptive stats, hypothesis testing, correlation, regression, ANOVA, chi-square, t-test, z-test, p-value, confidence interval), visualization (plot type, axes, labels, title, legend, color palette, figure size, subplots, annotations, style, export format), ML (model type, features, target, train/test split, hyperparameters, cross-validation, metrics, predictions, feature importance, pipeline, grid search)
- **R (tidyverse, ggplot2)** -- statistical computing (data frames, tibbles, dplyr verbs, tidyr, purrr, stringr, lubridate, forcats, readr), data visualization (ggplot, geom layers, aesthetics, facets, scales, themes, labels, annotations, coordinate systems, interactive with plotly), reporting (R Markdown, knitr, HTML/PDF/Word output, code chunks, inline code, parameters, YAML header, Shiny integration)
- **Jupyter Notebooks** -- interactive analysis (cells, code cells, markdown cells, output, kernel, language, execution order, cell metadata), visualization (inline plots, interactive widgets, HTML output, image output, JavaScript, ipywidgets), documentation (markdown, LaTeX, code comments, narrative text, table of contents, tags, metadata), reproducible research (environment, requirements, container, nbconvert, Binder, papermill, parameterized execution)
- **Google Colab** -- cloud notebooks (notebook ID, cells, runtime type, GPU/TPU, storage, connected drive, shared with, execution history), GPU access (GPU type, memory, utilization, runtime duration, session limits, pro/free tier), collaborative analysis (real-time editing, comments, sharing, version history, form fields, interactive outputs)

## Product & Web Analytics

- **Google Analytics 4** -- website traffic (users, new users, sessions, engaged sessions, engagement rate, average engagement time, events, conversions, revenue), user behavior (event name, event count, event parameters, page views, scroll events, outbound clicks, site search, file downloads, video engagement), conversions (conversion event, count, value, attribution, source, medium, campaign, path length, time lag, model comparison), audiences (audience name, conditions, membership duration, users, triggers, sequence, exclusion, export to ads), explorations (exploration type, dimensions, metrics, segments, filters, date comparison, cohort, funnel, path, free form)
- **Amplitude** -- product analytics (event name, properties, user properties, count, unique users, DAU, WAU, MAU, trends, segmentation), user journeys (pathfinder, event sequences, conversion paths, drop-off points, user flows, session replays), cohort analysis (cohort definition, retention curve, behavior over time, comparison, first event, return event, day/week/month), experiment results (experiment ID, variants, sample size, metric, baseline, treatment effect, confidence interval, significance, decision)
- **Mixpanel** -- event analytics (event name, properties, total events, unique users, frequency distribution, property breakdown, trends), funnels (funnel name, steps, conversion rate per step, overall conversion, time to convert, drop-off analysis, holding property, trending, segmentation), retention (retention event, return event, frequency, day/week/month, retention curve, segment comparison, churn rate), A/B test analysis (experiment, control, variants, metric, conversion rate, lift, confidence, sample size, duration, winner, segment analysis)
- **Heap** -- auto-captured event data (event type, element, page URL, timestamp, user, session, device, referrer, UTM, properties), retroactive analysis (event definition, historical data, funnel, retention, segment, date range, no prior instrumentation required), session replay (session ID, user, timestamp, duration, pages viewed, clicks, errors, frustration signals, replay URL)

## Survey & Customer Data

- **Qualtrics** -- survey data (survey ID, responses, questions, response values, completion rate, duration, channel, distribution, quota, randomization), experience management (XM program, touchpoints, journey, operational data, X-data, dashboard, action planning, closed-loop), statistical analysis (cross-tabulation, correlation, regression, significance testing, MaxDiff, conjoint, text analytics, sentiment, topic modeling), text analytics (open-end responses, topics, themes, sentiment, word frequency, parent/child topics, expert rules, machine learning)
- **SurveyMonkey** -- survey responses (response ID, collector, IP address, timestamp, completion status, page path, answers, score, weighted, custom variables), benchmarks (industry benchmark, question benchmark, global comparison, trending, percentile, sample size), sentiment analysis (question, response text, sentiment score, positive/negative/neutral, keywords, themes)
- **Medallia** -- customer experience data (signal ID, touchpoint, channel, timestamp, customer, location, product, experience score, text feedback), NPS (survey, response, score, promoter/passive/detractor, verbatim, driver analysis, trend, benchmark, segment), journey analytics (journey stage, touchpoints, experience scores, effort, sentiment, drop-off, recovery, cross-channel, time in stage)

## Data Governance & Catalog

- **Atlan** -- data catalog (asset type, name, description, owners, certifications, tags, classifications, custom metadata, popularity, related assets, lineage), lineage (upstream sources, downstream consumers, transformations, column-level lineage, impact analysis, lineage graph, cross-platform), governance (policies, classifications, PII detection, access controls, compliance tags, data quality rules, ownership), collaboration (announcements, discussions, mentions, requests, tasks, README, wiki, saved queries)
- **Alation** -- data catalog (data source, schema, table, column, description, steward, trust flag, popularity, top users, queries, joins, lineage), search (query, results, relevance, filters, object type, steward, certification, domain, tag), data governance (governance policies, stewardship, curation, flags, endorsement, deprecation, warning, compliance tags), stewardship (steward assignment, curation tasks, description completeness, trust flags, domain management, custom fields)
- **Monte Carlo** -- data observability (table, freshness, volume, schema, field health, distribution, dimension tracking, custom monitors), anomaly detection (incident ID, table, type, severity, anomaly description, expected vs actual, timestamp, status, root cause), lineage (table, upstream, downstream, query-based lineage, BI reports, data applications, impact radius, freshness inheritance), incident management (incident ID, severity, affected tables, root cause, resolution, notification, SLA, postmortem, owner)
- **Great Expectations** -- data quality testing (expectation suite, expectations, batch, validation result, success/failure, statistics, unexpected values, evaluated rows), validation (checkpoint, batch request, expectation suite, action list, results, data docs, slack notification, store), documentation (data docs site, expectation descriptions, validation results, profiling, column statistics, data overview, custom renderers)
