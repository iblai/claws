# Data Sources

Systems and platforms commonly accessed for engineering team support.

## Source Code & Version Control

- **GitHub** -- repositories (repo name, description, visibility, language, license, default branch, topics, stars, forks, last pushed, branch protection rules, CODEOWNERS), pull requests (PR number, title, description, author, reviewers, status, base/head branch, commits, changed files, additions, deletions, comments, review decisions, merge status, linked issues, CI checks), code review (review ID, reviewer, state, body, comments, requested changes, approval, thread resolution, suggestion commits), issues (issue ID, title, body, author, labels, assignee, milestone, project, state, comments, reactions, linked PRs, timeline events), Actions CI/CD (workflow ID, name, trigger, jobs, steps, status, duration, artifacts, logs, runner, matrix), Packages (package name, version, downloads, registry, visibility, repository link), Copilot (suggestions accepted, suggestions dismissed, completion language, IDE, lines suggested, acceptance rate)
- **GitLab** -- repositories (project ID, name, description, visibility, default branch, tags, CI config, container registry, wiki, pages), merge requests (MR ID, title, description, author, assignee, reviewers, source/target branch, commits, changes, pipeline status, approval rules, merge status, discussions, time tracking), CI/CD pipelines (pipeline ID, ref, status, stages, jobs, duration, artifacts, variables, manual actions, schedules, child pipelines), issues (issue ID, title, description, author, assignee, labels, milestone, weight, time tracking, linked MRs, related issues), container registry (image name, tags, size, last updated, cleanup policies, vulnerability scan results), security scanning (SAST, DAST, dependency scanning, container scanning, license compliance, security dashboard, vulnerability report)
- **Bitbucket (Atlassian)** -- repositories (repo slug, project, description, language, default branch, branch permissions, hooks, LFS), pull requests (PR ID, title, description, author, reviewers, source/destination branch, status, diff, commits, comments, approvals, merge checks, build status), Pipelines CI/CD (pipeline UUID, trigger, status, steps, duration, artifacts, deployment environment, variables, caches), Jira integration (issue key, branch, commit, PR, deployment, build status, development panel)
- **Azure DevOps Repos** -- Git repositories (repo ID, name, project, default branch, policies, size, forks, branch permissions), pull requests (PR ID, title, description, author, reviewers, source/target branch, status, policies, work items linked, iterations, votes, comments, merge type), branch policies (minimum reviewers, linked work items, build validation, comment resolution, merge strategy, status checks), code search (query, results, repository, file path, matched content, language, project scope)

## CI/CD & Build

- **GitHub Actions** -- workflow automation (workflow file, triggers, jobs, steps, matrix strategy, concurrency, permissions, timeout, environment), build/test/deploy pipelines (workflow run ID, status, conclusion, duration, triggered by, commit, branch, event, logs, annotations), reusable workflows (called workflow, inputs, outputs, secrets, caller workflows, version), matrix builds (matrix strategy, combinations, include/exclude, fail-fast, max-parallel, per-combination status)
- **GitLab CI/CD** -- pipelines (pipeline ID, ref, source, status, stages, created/started/finished, duration, user, detailed status), stages (stage name, jobs, status, duration, needs, dependencies, rules), runners (runner ID, type, status, tags, description, platform, version, jobs executed, queue time), environments (environment name, URL, state, last deployment, auto-stop, protected, tier), deployment tracking (deployment ID, environment, status, user, commit, pipeline, created date, finished date)
- **Jenkins** -- build pipelines (job name, build number, status, duration, timestamp, parameters, cause, artifacts, changelog, test results), plugin ecosystem (plugin name, version, dependencies, update available, category, enabled), distributed builds (node name, status, labels, executors, OS, offline/online, disk space, response time), pipeline-as-code (Jenkinsfile, stages, steps, parallel, post actions, shared libraries, parameters, triggers)
- **CircleCI** -- CI/CD pipelines (pipeline ID, project, branch, trigger, status, workflows, duration, credits used), orbs (orb name, version, commands, jobs, executors, usage, source), caching (cache key, paths, size, hit rate, restore/save time), parallelism (test splitting, containers, timing data, balance, wall time vs compute time), insights (workflow, success rate, duration, throughput, credits, failure reasons, flaky tests)
- **ArgoCD** -- GitOps continuous delivery (application name, project, source repo, source path, destination cluster, destination namespace, sync status, health status), Kubernetes deployment (application, resources, sync status, health, manifest, live state, diff, events), sync management (sync policy, automated sync, prune, self-heal, retry, sync window, sync status, sync result, revision)

## Project & Issue Tracking

- **Jira (Atlassian)** -- issues (issue key, project, type, summary, description, status, priority, assignee, reporter, created, updated, due date, components, labels, fix version, custom fields, story points), sprints (sprint ID, board, name, state, start date, end date, goal, issues, completed story points, burndown), boards (board ID, name, type, project, columns, filters, swim lanes, estimation, card layout), epics (epic key, name, summary, status, stories, story points, start/end date, color, priority), roadmaps (roadmap view, timeline, child issues, dependencies, progress, status categories), automation (rule ID, trigger, conditions, actions, execution log, audit, smart values), custom workflows (workflow name, statuses, transitions, conditions, validators, post-functions, screens)
- **Linear** -- issues (issue ID, identifier, title, description, status, priority, assignee, team, project, label, cycle, estimate, due date, created date, parent, sub-issues, relations, SLA), cycles (cycle ID, number, name, team, start date, end date, issues, completed issues, scope change, progress, velocity), projects (project ID, name, description, status, lead, members, target date, milestones, issues, updates, progress), roadmaps (initiative, projects, timeline, dependencies, status, priority, team ownership), triage (triage state, untriaged issues, team, SLA, auto-assignment rules, bug/feature classification), automations (automation ID, trigger, action, conditions, team scope, execution history)
- **GitHub Issues / Projects** -- issue tracking (issue number, title, body, author, labels, assignee, milestone, state, comments, reactions, linked PRs, timeline, transferred, locked), project boards (project ID, title, description, fields, views, items, status, priority, iteration, custom fields, workflows), milestones (milestone ID, title, due date, description, open issues, closed issues, progress, state)
- **Shortcut (formerly Clubhouse)** -- stories (story ID, name, description, type, state, estimate, owners, labels, epic, iteration, project, workflow, tasks, comments, branches, PRs), epics (epic ID, name, objective, state, stories, milestones, planned/started/completed dates, labels, owners), iterations (iteration ID, name, start/end date, stories, capacity, velocity, burndown, group), milestones (milestone ID, name, state, started/completed date, epics, categories)

## Documentation

- **Confluence** -- technical documentation (page ID, title, body, space, labels, version, author, last modified, restrictions, child pages, comments, likes, macros), architecture decision records ADRs (ADR number, title, status, context, decision, consequences, date, author, supersedes), runbooks (runbook ID, service, scenario, steps, escalation path, rollback procedure, last tested, owner, related incidents), retrospectives (retro page, sprint, what went well, what could improve, action items, participants, date)
- **Notion** -- engineering wikis (workspace, pages, databases, templates, permissions, integrations, favorites), RFCs (RFC ID, title, status, author, reviewers, problem statement, proposed solution, alternatives, decision, date), project documentation (project page, requirements, design docs, technical specs, meeting notes, status updates, timeline), databases (database ID, properties, views, relations, rollups, formulas, automations, API access)
- **ReadMe** -- API documentation (API name, version, endpoints, methods, parameters, request/response schemas, authentication, code samples, try-it), developer hubs (project, categories, pages, guides, tutorials, recipes, changelog, custom pages), changelogs (entry ID, title, body, type, version, date, category, hidden), metrics (API calls, error rate, popular endpoints, time to first call, active users, documentation views, search terms)
- **Backstage (Spotify)** -- developer portal (catalog entities, templates, plugins, search, integrations, authentication, permissions), service catalog (component name, type, lifecycle, owner team, system, dependencies, API specs, documentation, metadata, annotations, relations), TechDocs (doc site, mkdocs config, content, navigation, search, last built, owner, related entity), API specs (API name, type, definition URL, owner, system, lifecycle, consumers, provided by, spec format)

## Infrastructure & Cloud

- **AWS** -- EC2 (instance ID, type, state, AMI, VPC, subnet, security groups, IAM role, public/private IP, tags, monitoring), S3 (bucket name, region, size, objects, versioning, encryption, lifecycle rules, access policy, replication), RDS (DB identifier, engine, version, instance class, storage, multi-AZ, read replicas, backup, parameter group), Lambda (function name, runtime, memory, timeout, handler, layers, triggers, invocations, errors, duration, throttles), CloudWatch (alarms, metrics, logs, dashboards, events, anomaly detection, composite alarms, metric math), IAM (users, groups, roles, policies, access keys, MFA, permission boundaries, last accessed), EKS (cluster name, version, node groups, Fargate profiles, add-ons, networking, logging, authentication), CloudFormation/CDK (stack name, template, resources, parameters, outputs, status, events, drift, change sets)
- **Google Cloud Platform** -- Compute Engine (instance name, machine type, zone, status, network, disk, image, metadata, labels, preemptible), GKE (cluster name, version, node pools, autoscaling, networking, security, monitoring, add-ons), Cloud Run (service name, region, image, revision, traffic split, concurrency, min/max instances, authentication), BigQuery (dataset, tables, views, schema, partitioning, clustering, query history, cost, slots), Cloud Functions (function name, runtime, trigger, entry point, memory, timeout, VPC connector, environment variables, invocations)
- **Microsoft Azure** -- VMs (VM name, size, OS, location, resource group, availability set/zone, managed disks, NSG, public IP, tags), AKS (cluster name, version, node pools, networking, monitoring, RBAC, managed identity, add-ons), Azure Functions (function app, functions, runtime, trigger, bindings, plan, configuration, application insights), Cosmos DB (account, databases, containers, partition key, throughput, consistency, replication, indexing policy), Azure Monitor (metrics, logs, alerts, workbooks, dashboards, Application Insights, Log Analytics, diagnostic settings), ARM/Bicep (template, parameters, resources, outputs, deployment, what-if, resource group, subscription)
- **Terraform** -- infrastructure-as-code (configuration files, providers, resources, data sources, variables, outputs, locals, modules), state management (state file, remote backend, locking, workspace, resource addresses, serial, lineage), module registry (module name, provider, version, inputs, outputs, dependencies, documentation, examples), plan/apply (plan output, resource changes, add/change/destroy counts, drift detection, refresh, targeted apply)
- **Pulumi** -- infrastructure-as-code (program language, project, stack, resources, providers, configuration, secrets), state management (state backend, stack history, resource URNs, outputs, pending operations, checkpoints)

## Monitoring & Observability

- **Datadog** -- APM (service, operation, latency, error rate, throughput, traces, spans, dependencies, service map, flame graph), infrastructure monitoring (host, CPU, memory, disk, network, processes, containers, integrations, tags), logs (log source, message, level, service, tags, attributes, facets, patterns, indexes, archives, pipelines), dashboards (dashboard ID, title, widgets, queries, variables, sharing, SLOs, annotations), alerting (monitor ID, name, type, query, threshold, notification, escalation, mute, downtime, status), RUM (application, page views, user sessions, errors, performance, core web vitals, user actions, long tasks), profiling (service, language, profile type, CPU, memory, wall time, lock contention, hotspots)
- **Grafana** -- dashboards (dashboard ID, title, panels, queries, variables, annotations, templating, sharing, playlists), alerting (alert rule, condition, notification channel, silence, mute timing, contact point, evaluation), Prometheus data (metric name, labels, type, range query, instant query, PromQL, recording rules, alerting rules), Loki data (log stream, labels, LogQL query, pattern, parser, derived fields), Tempo data (trace ID, spans, service, operation, duration, errors, service graph)
- **PagerDuty** -- incident management (incident ID, title, description, status, urgency, priority, service, escalation policy, assigned to, created date, acknowledged date, resolved date, timeline), on-call scheduling (schedule ID, name, rotation, escalation level, overrides, layers, time zone, on-call user), escalation policies (policy ID, name, levels, targets, repeat, handoff notification, timeout), postmortems (incident, timeline, impact, root cause, contributing factors, remediation items, follow-ups, lessons learned)
- **Sentry** -- error tracking (issue ID, title, culprit, level, platform, first seen, last seen, events count, users affected, status, assignee, tags), performance monitoring (transaction name, duration, throughput, failure rate, apdex, web vitals, spans, slow operations), release tracking (release version, projects, new issues, resolved issues, sessions, crash rate, adoption, commits, deploys), issue assignment (issue, assignee, suspect commits, ownership rules, auto-assignment, alert rules)
- **New Relic** -- APM (application, response time, throughput, error rate, apdex, transactions, external services, databases, JVM/runtime), infrastructure (host, CPU, memory, disk, network, processes, containers, integrations, system events), logs (log source, message, level, attributes, parsing rules, patterns, drop rules, obfuscation), browser monitoring (page views, page load time, AJAX, JavaScript errors, session traces, SPA monitoring, core web vitals), synthetics (monitor name, type, locations, frequency, status, duration, failures, SLA, script)

## Security

- **Snyk** -- dependency vulnerability scanning (vulnerability ID, CVE, severity, CVSS, package, version, fixed version, exploit maturity, reachability, priority score), container scanning (image, base image, OS vulnerabilities, application vulnerabilities, Dockerfile issues, recommendations), IaC scanning (file path, resource, issue, severity, documentation, remediation, policy, line number), license compliance (package, license, severity, dependency path, policy, project, organization)
- **SonarQube / SonarCloud** -- static code analysis (project, bugs, vulnerabilities, code smells, coverage, duplications, complexity, debt, quality gate status), code smells (rule ID, severity, effort, message, file, line, tags, clean code attribute), security hotspots (hotspot ID, rule, file, line, status, review priority, vulnerability probability, assignee, resolution), quality gates (gate name, conditions, metric, operator, threshold, status, project association)
- **Dependabot (GitHub)** -- automated dependency updates (PR number, dependency, from version, to version, compatibility score, release notes, changelog, auto-merge eligibility), security alerts (alert ID, CVE, severity, package, affected versions, patched version, state, dismissed reason, fixed PR), version updates (ecosystem, directory, schedule, open PRs, merged PRs, ignored dependencies, groups)
- **Trivy** -- container image scanning (image, OS, OS version, vulnerabilities by severity, packages, CVE ID, fixed version, layer), filesystem scanning (path, vulnerabilities, misconfigurations, secrets, licenses, severity counts), IaC scanning (file path, resource, type, misconfigurations, severity, message, resolution, references)
- **HashiCorp Vault** -- secrets management (secret engine, path, key, version, metadata, created time, deletion time, destroyed, cas), dynamic credentials (role, TTL, max TTL, backend, generated credentials, lease ID, renewable, revocation), encryption-as-a-service (transit engine, key name, type, derived, convergent, exportable, allow plaintext backup, min decryption/encryption version)

## Package Registries

- **npm (Node.js)** -- package publishing (package name, version, description, main, scripts, dependencies, devDependencies, peerDependencies, license, repository, keywords), version management (published versions, dist-tags, latest, next, deprecation, download count, publish date), scoped packages (scope, package name, access level, team, members, 2FA requirement)
- **PyPI** -- Python package index (package name, version, summary, author, license, classifiers, requires-python, dependencies, project URLs), publishing (sdist, wheel, platform, python version, upload date, size, md5 digest, downloads), version management (releases, pre-releases, yanked versions, changelog, compatible versions)
- **Maven Central / Artifactory** -- Java/JVM packages (group ID, artifact ID, version, packaging, classifier, dependencies, scope, optional, repository), artifact management (repository type, layout, artifacts, size, last modified, checksums, metadata, properties, replication), build proxying (remote repository, cache, proxy settings, excludes, retrieval count, bandwidth, cleanup)
- **Docker Hub / GitHub Container Registry / ECR** -- container image storage (image name, tag, digest, size, OS, architecture, layers, pushed date, pull count, vulnerability scan), scanning (scan status, findings count, critical/high/medium/low, CVE list, remediation, rescan date), distribution (repository, visibility, automated builds, webhooks, access tokens, organizations, teams, permissions)
